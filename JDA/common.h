#pragma once

#include <Windows.h>
#include <iostream>
#include <stdio.h>
#include <string>
#include <vector>
#include <filesystem>
#include <unordered_map>
#include <zip.h> // vcpkg install libzip:x64-windows && vcpkg integrate install
#include <fstream>

typedef uint8_t u1;
typedef uint16_t u2;
typedef uint32_t u4;
typedef uint64_t u8;